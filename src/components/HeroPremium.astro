<section class="hero-premium">
	<div class="hero-grid">
		<div class="hero-text">
			<h1>Marc Michon</h1>

			<p class="role">Cybersecurity Engineer</p>

			<div class="champion">
				<img src="/Images/R7.svg" alt="Rapid7" />
				<span>Rapid7 Tech Champion</span>
			</div>

			<p class="intro">
				Architecture, intégration et exploitation de plateformes de cybersécurité
				pour améliorer la visibilité, la détection et la réduction de la surface d’attaque.
			</p>

			<div class="actions">
				<a href="/notes-de-terrain">Notes de terrain</a>
				<a href="mailto:michon.marc@gmail.com" class="secondary" target="_blank">
					Me contacter
				</a>
			</div>
		</div>

		<div class="hero-photo">
			<img src="/Images/moi.jpeg" alt="Marc Michon" />
		</div>

		<div class="hero-domains">
			<div>Vulnerability<br />Management</div>
			<div>Exposure<br />Management</div>
			<div>Security<br />Operations</div>
			<div>Threat<br />Intelligence</div>
		</div>
	</div>
</section>
<style>
	.hero-premium {
		background:
			radial-gradient(circle at 35% 40%, rgba(37, 99, 235, 0.35), transparent 30%),
			linear-gradient(135deg, #020817 0%, #06142d 45%, #031b4d 100%);
		color: white;
		padding: 5rem 4rem 6.5rem;
		position: relative;
		overflow: hidden;
	}

	.hero-grid {
		max-width: 1320px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1.6fr 0.95fr 0.8fr;
		align-items: center;
		gap: 4rem;
	}

	.hero-text {
		color: white;
	}

	h1 {
		font-size: 5.7rem;
		font-weight: 900;
		line-height: 0.95;
		margin: 0;
		letter-spacing: -0.05em;
		color: white;
		white-space: nowrap;
	}

	.role {
		font-size: 2.8rem;
		font-weight: 800;
		color: #3b82f6;
		margin: 0.8rem 0 1.4rem;
		line-height: 1.1;
	}

	.champion {
		display: flex;
		align-items: center;
		gap: 0.65rem;
		color: #60a5fa;
		font-size: 1.5rem;
		font-weight: 800;
		margin: 0 0 2.2rem;
	}

	.champion img {
		height: 26px;
		width: auto;
		display: block;
	}

	.intro {
		max-width: 640px;
		color: #dbeafe;
		font-size: 1.18rem;
		line-height: 1.85;
		margin: 0;
	}

	.actions {
		display: flex;
		gap: 1rem;
		margin-top: 2.3rem;
	}

	.actions a {
		background: #2563eb;
		color: white;
		padding: 1rem 1.35rem;
		border-radius: 10px;
		text-decoration: none;
		font-weight: 800;
	}

	.actions a.secondary {
		background: transparent;
		border: 1px solid rgba(255, 255, 255, 0.35);
	}

	.hero-photo img {
		width: 340px;
		height: 420px;
		object-fit: cover;
		border-radius: 30px;
		filter: drop-shadow(0 35px 70px rgba(0, 0, 0, 0.5));
		display: block;
	}

	.hero-domains {
		display: grid;
		gap: 1.1rem;
		font-weight: 800;
		text-transform: uppercase;
		font-size: 0.92rem;
		color: #e5edff;
	}

	.hero-domains div {
		border-left: 2px solid #2563eb;
		padding: 1.15rem;
		background: rgba(255, 255, 255, 0.04);
		border-radius: 14px;
	}

	@media (max-width: 1100px) {
		.hero-grid {
			grid-template-columns: 1fr 0.8fr;
		}

		.hero-domains {
			display: none;
		}

		h1 {
			font-size: 4.8rem;
		}
	}

	@media (max-width: 900px) {
		.hero-premium {
			padding: 4rem 1.5rem;
		}

		.hero-grid {
			grid-template-columns: 1fr;
		}

		h1 {
			font-size: 3.4rem;
			white-space: normal;
		}

		.role {
			font-size: 2rem;
		}

		.hero-photo img {
			width: 100%;
			height: auto;
		}
	}
</style>