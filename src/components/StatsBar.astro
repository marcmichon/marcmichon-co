<section class="stats-bar">
	<div class="stat">
		<strong>10+</strong>
		<span>ans d’expérience</span>
	</div>

	<div class="stat">
		<strong>Rapid7</strong>
		<span>Tech Champion</span>
	</div>

	<div class="stat">
		<strong>20+</strong>
		<span>programmes VM accompagnés</span>
	</div>

	<div class="stat">
		<strong>100K+</strong>
		<span>assets analysés</span>
	</div>

	<div class="stat">
		<strong>MSSP / SOC</strong>
		<span>environnements complexes</span>
	</div>
</section>

<style>
	.stats-bar {
		max-width: 1120px;
		margin: -3rem auto 1.5rem;
		padding: 1.5rem 2rem;
		background: linear-gradient(135deg, #06142d, #020817);
		color: white;
		border-radius: 14px;
		display: grid;
		grid-template-columns: repeat(5, 1fr);
		gap: 1.5rem;
		box-shadow: 0 20px 50px rgba(0,0,0,.25);
		position: relative;
		z-index: 5;
	}

	.stat {
		border-right: 1px solid rgba(255,255,255,.12);
	}

	.stat:last-child {
		border-right: none;
	}

	.stat strong {
		display: block;
		font-size: 1.6rem;
	}

	.stat span {
		color: #cbd5e1;
		font-size: 0.85rem;
	}

	@media (max-width: 900px) {
		.stats-bar {
			grid-template-columns: 1fr;
			margin: 0 1.5rem 3rem;
		}

		.stat {
			border-right: none;
			border-bottom: 1px solid rgba(255,255,255,.12);
			padding-bottom: 1rem;
		}
	}
</style>